repos:
  - repo: local
    hooks:
      - id: golines
        name: golines
        files: ".+.go$"
        language: system
        entry: |
          sh -c 'export PATH=/usr/local/go/bin:/usr/bin:/usr/local/bin:$HOME/go/bin:$PATH; go run github.com/segmentio/golines -w . || exit 0'
      - id: goimports
        name: goimports
        files: ".+.go$"
        language: system
        entry: |
          sh -c 'export PATH=/usr/local/go/bin:/usr/bin:/usr/local/bin:$HOME/go/bin:$PATH; go run golang.org/x/tools/cmd/goimports@latest -w . || exit 0'

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: no-commit-to-branch
        args: ['--branch', 'master']

  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.33.0
    hooks:
      - id: check-dependabot
      - id: check-github-actions
      - id: check-github-workflows

  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.9.0
    hooks:
      - id: zizmor
